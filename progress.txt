# Ralph Progress Log
Started: Fri Apr 24 17:37:24 PDT 2026
---

## Codebase Patterns
- vitest works with SvelteKit's vite config out of the box — `npm test` runs `vitest run`, `npm run test:watch` for watch mode
- MiniMax validator is the single source of truth for request validation: `src/lib/providers/minimax/validateMusicRequest.ts`
- Supported MiniMax structure tags: [Intro], [Verse], [Verse 1], [Verse 2], [Pre-Chorus], [Chorus], [Hook], [Bridge], [Breakdown], [Solo], [Outro]
- Use `npx shadcn-svelte add <component> --no-deps --overwrite` and pipe `echo "y"` for confirmation to add shadcn-svelte components
- shadcn-svelte v1.2.7 presets: nova, vega, maia, lyra, mira, luma (not "default"/"new-york")
- `WithElementRef` type must be exported from `$lib/utils.ts` for shadcn-svelte components
- Tailwind CSS v4 uses `@tailwindcss/vite` plugin (not PostCSS) and `@theme inline` blocks for design tokens
- shadcn-svelte CSS uses oklch color format in `:root` / `.dark` selectors
- `tw-animate-css` is required for shadcn-svelte animations
- Project uses `@sveltejs/adapter-cloudflare` with `pages_build_output_dir: ".svelte-kit/cloudflare"` in wrangler.jsonc
- Drizzle ORM schema lives at `src/lib/server/db/schema.ts`; db client factory at `src/lib/server/db/index.ts`
- Local dev uses `pg` driver via `createLocalDb()`; production uses `@neondatabase/serverless` via `createNeonDb()`
- `drizzle.config.ts` reads `DATABASE_URL` from `process.env` (used by drizzle-kit CLI)
- Local Postgres database is `grande_studio` on localhost:5432
- Schema file must have at least `export {}` to be a valid TypeScript module (Drizzle requires it)
- Use `.$type<EnumType>()` on text columns for typed enums in Drizzle (keeps DB flexible, TS strict)
- Drizzle index definitions use a second arg function to `pgTable()` returning an array
- Exported type aliases (SourceType, Provider, AssetStatus, BranchType) from schema.ts are reusable across services
- Use `check()` from `drizzle-orm/pg-core` + `sql` from `drizzle-orm` for CHECK constraints in table definitions
- Custom SQL migrations (RLS, triggers, etc.): use `drizzle-kit generate --custom --name=<name>` then apply with `psql -d grande_studio -f <file>` (not `drizzle-kit push`)
- RLS helper `withRLS(db, userId, fn)` at `src/lib/server/db/rls.ts` wraps DB operations in a transaction with `SET LOCAL app.user_id`
- BetterAuth auth-schema lives at `src/lib/server/db/auth-schema.ts` — managed by BetterAuth, do not rename columns
- BetterAuth Drizzle adapter: use `drizzleAdapter(db, { provider: 'pg', schema: authSchema })` — pass the auth schema explicitly
- BetterAuth CLI `migrate` doesn't work with Drizzle adapter — use `npx @better-auth/cli generate` then `drizzle-kit generate` + `drizzle-kit push`
- BetterAuth reads `BETTER_AUTH_SECRET` and `BETTER_AUTH_URL` from `process.env` automatically
- Auth server at `src/lib/server/auth.ts`; auth client at `src/lib/auth-client.ts`; API route at `src/routes/api/auth/[...all]/+server.ts`
- `drizzle.config.ts` schema field accepts an array of paths for multiple schema files
- BetterAuth Svelte client API: `authClient.signUp.email({ email, password, name })` and `authClient.signIn.email({ email, password })` — both return `{ data, error }`
- shadcn-svelte Card uses namespace import: `import * as Card from '$lib/components/ui/card'`
- MiniMax adapter factory: `createMiniMaxAdapter(apiKey)` at `src/lib/providers/minimax/adapter.ts` returns `MusicProvider` interface
- MiniMax API endpoint: `https://api.minimaxi.chat/v1/music_generation`, model: `music-01`
- MiniMax request fields: `is_instrumental` (bool), `lyrics` (optional), `refer_voice` (source URL for cover), `lyrics_optimization` (bool)
- Payload builders exported separately from adapter for direct unit testing without fetch mocking
- Cover/re-style source URLs must be from R2, not provider CDNs — `validateR2SourceUrl()` in adapter.ts enforces this
- Known provider CDN domains blocked: `cdn.minimax.chat`, `cdn.minimaxi.chat`, `fileserviceupload.minimax.chat`
- State machine services at `src/lib/services/` are pure functions with no DB dependency — use `validateAssetTransition` and `validateJobTransition` before any status update
- `ProviderError` and `ProviderErrorCode` types at `src/lib/providers/types.ts` — all adapter methods normalize errors at the boundary
- `decodeHexToBytes(hex)` at adapter.ts decodes hex-encoded audio to Uint8Array; strips whitespace, validates even length
- `normalizeMiniMaxError(error)` at adapter.ts maps MiniMaxApiError → ProviderError: 401/403→auth, 429→rate_limited, 408/502/503/504→timeout, 400/1000+→validation
- `parseStreamLines(body)` at adapter.ts parses SSE or JSON-line streaming responses into hex audio chunks
- Adapter methods now throw `ProviderError` (not `MiniMaxApiError`) — MiniMaxApiError is internal only
- Quota service uses repository pattern: `QuotaRepository` interface for DB abstraction, `createQuotaService(repo)` factory for business logic, `createDrizzleQuotaRepository(db)` for Drizzle implementation
- `validateQuotaTransition(current, next)` at `src/lib/services/quota.ts` — pure function like asset-status/job-status
- Quota constants: `QUOTA_TTL_MS = 600000` (10 min), `DAILY_LIMIT = 10`
- `QuotaServiceResult` is `{ ok: true; reservation } | { ok: false; error: string }` — not exceptions
- Daily usage counted by committed reservations updated after today's midnight UTC
- Take edge service at `src/lib/services/take-edges.ts` uses same repository pattern as quota service: `TakeEdgeRepository` interface + `createTakeEdgeService(repo)` factory + `createDrizzleTakeEdgeRepository(db)` for Drizzle
- `TakeEdgeServiceResult` is `{ ok: true; edge } | { ok: false; error: string }` — consistent with QuotaServiceResult pattern
- Cycle detection uses BFS from child asset following parent→child edges; if proposed parent is reachable, the edge would create a cycle
- Soft-delete for edges: `softDeleteByAssetId` sets `deletedAt` on all edges where the asset is parent or child; all queries filter by `isNull(deletedAt)`

## 2026-04-24 18:07 - US-001
- Scaffolded SvelteKit 2 project with Svelte 5, TypeScript, Tailwind CSS v4, shadcn-svelte (vega/neutral), and Cloudflare Pages adapter
- Files changed:
  - package.json (created with all dependencies)
  - svelte.config.js (Cloudflare adapter, runes mode)
  - vite.config.ts (Tailwind CSS v4 vite plugin)
  - tsconfig.json (TypeScript strict config)
  - wrangler.jsonc (Cloudflare Pages config)
  - components.json (shadcn-svelte config)
  - src/app.css (Tailwind + shadcn-svelte theme with oklch neutral palette)
  - src/app.html, src/app.d.ts (SvelteKit app shell)
  - src/lib/utils.ts (cn helper + WithElementRef type)
  - src/lib/components/ui/button/ (shadcn-svelte button component)
  - src/routes/+layout.svelte, +page.svelte (root layout with CSS import)
  - .gitignore, .npmrc, static/favicon.svg
- **Learnings for future iterations:**
  - shadcn-svelte CLI is stubbornly interactive; use `echo "y" | npx shadcn-svelte add ...` for automation
  - The `--no-deps` flag prevents auto-installing peer deps (useful when managing deps manually)
  - shadcn-svelte components may import types not in the default utils.ts (e.g., WithElementRef)
  - Dev server starts on port 5173 by default
  - Typecheck: `npm run check` (runs svelte-kit sync + svelte-check)
---

## 2026-04-24 - US-002
- Installed drizzle-orm, drizzle-kit, pg, @types/pg, @neondatabase/serverless
- Created `drizzle.config.ts` reading DATABASE_URL from env, pointing at `src/lib/server/db/schema.ts`
- Created `src/lib/server/db/index.ts` with dual driver support: `createLocalDb()` (node-postgres) and `createNeonDb()` (@neondatabase/serverless)
- Created `src/lib/server/db/schema.ts` (empty module placeholder for future tables)
- Updated `.env` with `DATABASE_URL=postgresql://localhost:5432/grande_studio`
- Created `.env.example` with placeholder values
- Created local Postgres database `grande_studio`
- Verified connection with `drizzle-kit generate` and `drizzle-kit push`
- Added npm scripts: `db:generate`, `db:push`, `db:studio`
- Files changed: package.json, package-lock.json, drizzle.config.ts, src/lib/server/db/index.ts, src/lib/server/db/schema.ts, .env, .env.example
- **Learnings for future iterations:**
  - Schema file must export something (at least `export {}`) to be a valid TS module — otherwise `import * as schema` fails
  - `drizzle-kit push` uses the `pg` driver automatically when dialect is `postgresql`
  - Both `createLocalDb` and `createNeonDb` return compatible Drizzle instances — use the `Database` union type for code that works with either
  - The `drizzle/` output directory is created by `drizzle-kit generate` for migrations
---

## 2026-04-24 18:14 - US-003
- Defined `projects` and `audio_assets` tables in Drizzle schema at `src/lib/server/db/schema.ts`
- projects: id (uuid PK), owner_id, title, created_at, updated_at, deleted_at
- audio_assets: id (uuid PK), project_id (FK), owner_id, source_type, provider, provider_model, provider_job_id, title, prompt, lyrics, structure_tags_json, input_asset_ids_json, r2_object_key, duration_sec, format, sample_rate, bit_depth, status, error_json, created_at, updated_at, deleted_at
- Indexes on audio_assets: owner_id, project_id
- Generated migration `drizzle/0000_organic_red_hulk.sql`
- Pushed migration to local Postgres — both tables created successfully
- Files changed: src/lib/server/db/schema.ts, drizzle/0000_organic_red_hulk.sql, drizzle/meta/
- **Learnings for future iterations:**
  - Use `.$type<EnumType>()` on text columns for typed enum values in Drizzle (not pgEnum for flexibility)
  - Indexes are defined as a second argument function to `pgTable()` returning an array
  - `drizzle-kit generate` creates the first migration as `0000_*.sql` — subsequent stories will generate incremental migrations
  - `drizzle-kit push` applies schema directly to local DB (no migration tracking table needed for dev)
  - Exported type aliases (SourceType, Provider, AssetStatus) make it easy for services to reference allowed values
---

## 2026-04-24 - US-004
- Defined `arrangement_clips` and `take_edges` tables in Drizzle schema at `src/lib/server/db/schema.ts`
- arrangement_clips: id (uuid PK), project_id (FK), owner_id, asset_id (FK), start_time_sec (default 0), trim_start_sec (default 0), trim_end_sec (nullable), clip_duration_sec (not null), gain_db (default 0), muted (default false), soloed (default false), layer_order (default 0), created_at, updated_at, deleted_at
- take_edges: id (uuid PK), project_id (FK), owner_id, parent_asset_id (FK), child_asset_id (FK), branch_type (typed enum), branch_prompt (nullable), created_at, deleted_at
- take_edges has CHECK constraint: `parent_asset_id <> child_asset_id`
- Indexes on arrangement_clips: owner_id, project_id, asset_id
- Indexes on take_edges: owner_id, project_id, parent_asset_id, child_asset_id
- Generated migration `drizzle/0001_oval_sue_storm.sql`
- Pushed migration to local Postgres — both tables created successfully
- Files changed: src/lib/server/db/schema.ts, drizzle/0001_oval_sue_storm.sql, drizzle/meta/
- **Learnings for future iterations:**
  - Import `boolean` and `check` from `drizzle-orm/pg-core`, and `sql` from `drizzle-orm` for CHECK constraints
  - Drizzle `check()` helper goes in the table's third-arg array alongside indexes
  - Use `sql` template literal for CHECK constraint expressions referencing table columns
  - BranchType enum follows same pattern as SourceType/Provider/AssetStatus — const array + $type<>()
---

## 2026-04-24 - US-005
- Defined `generation_jobs`, `quota_reservations`, `export_jobs`, and `provider_events` tables in Drizzle schema at `src/lib/server/db/schema.ts`
- generation_jobs: id (uuid PK), project_id (FK), owner_id, provider, provider_model, job_type (typed enum), status (typed enum), request_json (jsonb not null), response_json, error_json, error_code, quota_reservation_id, resulting_asset_id (FK), idempotency_key, created_at, updated_at
- quota_reservations: id (uuid PK), owner_id, generation_job_id (FK), idempotency_key, units_reserved, status (typed enum), expires_at, created_at, updated_at
- export_jobs: id (uuid PK), project_id (FK), owner_id, export_type (typed enum), status (typed enum), snapshot_version (default 1), arrangement_snapshot_json, r2_object_key, error_json, created_at, updated_at
- provider_events: id (uuid PK), owner_id, generation_job_id (FK), provider, event_type, payload_json, created_at
- Indexes on generation_jobs: owner_id, project_id, resulting_asset_id, idempotency_key
- Indexes on quota_reservations: owner_id, idempotency_key
- Indexes on export_jobs: owner_id, project_id
- Indexes on provider_events: owner_id, generation_job_id
- Generated migration `drizzle/0002_aromatic_harrier.sql`
- Pushed migration to local Postgres — all four tables created successfully
- Files changed: src/lib/server/db/schema.ts, drizzle/0002_aromatic_harrier.sql, drizzle/meta/
- **Learnings for future iterations:**
  - New type enums follow same pattern: `const array as const` + `type Alias = (typeof array)[number]` + `.$type<Alias>()`
  - New types added: JobType, JobStatus, QuotaStatus, ExportType, ExportStatus — all reusable across services
  - provider_events table has no updated_at (events are immutable/append-only)
  - quota_reservations references generation_jobs (FK), and generation_jobs references audio_assets (FK) — careful with table definition order in schema.ts
---

## 2026-04-24 - US-006
- Enabled Row Level Security (RLS) on all 8 user-owned tables: projects, audio_assets, arrangement_clips, take_edges, generation_jobs, quota_reservations, export_jobs, provider_events
- Created SELECT/INSERT/UPDATE/DELETE policies on each table referencing `current_setting('app.user_id', true)::uuid` against `owner_id`
- Created `src/lib/server/db/rls.ts` helper with `withRLS(db, userId, fn)` that wraps queries in a transaction with `SET LOCAL app.user_id`
- Custom SQL migration at `drizzle/0003_rls_ownership_policies.sql` (generated via `drizzle-kit generate --custom`)
- Applied migration to local Postgres — 8 ALTER TABLE + 32 CREATE POLICY all succeeded
- Files changed: drizzle/0003_rls_ownership_policies.sql, drizzle/meta/_journal.json, drizzle/meta/0003_snapshot.json, src/lib/server/db/rls.ts
- **Learnings for future iterations:**
  - Drizzle doesn't have first-class RLS support — use `drizzle-kit generate --custom --name=<name>` to create empty SQL migration files for custom DDL
  - `drizzle-kit push` only applies schema-level changes (tables, columns, indexes), NOT custom SQL migrations — use `psql -d grande_studio -f <migration_file>` to apply custom migrations
  - RLS policies use `current_setting('app.user_id', true)::uuid` — the `true` arg makes it return NULL (not error) if the setting is missing, so RLS denies all access by default (fail-closed)
  - `SET LOCAL app.user_id = '<uuid>'` scopes the setting to the current transaction — use `withRLS()` helper from `src/lib/server/db/rls.ts` to wrap DB operations
  - The table owner (typically the connection role) bypasses RLS by default — in production, use a restricted role or set `ALTER TABLE ... FORCE ROW LEVEL SECURITY` if the app connects as the table owner
---

## 2026-04-24 - US-007
- Installed better-auth v1.6.9
- Configured auth server at `src/lib/server/auth.ts` with email/password provider and Drizzle adapter
- Created BetterAuth Drizzle schema at `src/lib/server/db/auth-schema.ts` (user, session, account, verification tables)
- Updated `drizzle.config.ts` to include both app schema and auth schema (array of paths)
- Created auth API route handler at `src/routes/api/auth/[...all]/+server.ts` using `toSvelteKitHandler`
- Created auth client at `src/lib/auth-client.ts` using `createAuthClient` from `better-auth/svelte`
- Auto-generated `BETTER_AUTH_SECRET` via `openssl rand -base64 32` and set `BETTER_AUTH_URL=http://localhost:5173` in `.env`
- Added both vars to `.env.example` as placeholders
- Generated Drizzle migration `drizzle/0004_organic_shotgun.sql` for auth tables and pushed to local Postgres
- Files changed: package.json, package-lock.json, src/lib/server/auth.ts, src/lib/server/db/auth-schema.ts, src/lib/auth-client.ts, src/routes/api/auth/[...all]/+server.ts, .env, .env.example, drizzle.config.ts, drizzle/0004_organic_shotgun.sql, drizzle/meta/*
- **Learnings for future iterations:**
  - BetterAuth CLI `migrate` does NOT work with the Drizzle adapter — use `npx @better-auth/cli generate` to get the schema, then `drizzle-kit generate` + `drizzle-kit push`
  - The CLI generates `auth-schema.ts` at the repo root by default — move it to `src/lib/server/db/auth-schema.ts`
  - BetterAuth auth tables use singular names (user, session, account, verification) with text IDs — not UUIDs
- BetterAuth server-side session: `auth.api.getSession({ headers: request.headers })` returns `{ user, session } | null`
- SvelteKit hooks.server.ts populates `event.locals.user` and `event.locals.session` for all routes
- Protected routes use `PROTECTED_PREFIXES` array in hooks.server.ts; redirect to `/signin` if no session
- Root `/` allows unauthenticated access (temp project support); `/projects` prefix is protected
- `authClient.signOut()` from `better-auth/svelte` ends session client-side; redirect to `/signin` after
- Layout server load passes `locals.user` to all pages via `data.user`
  - The Drizzle adapter accepts `schema` option to pass the auth table definitions
  - `toSvelteKitHandler` from `better-auth/svelte-kit` wraps the auth handler for SvelteKit route handlers
  - `createAuthClient` from `better-auth/svelte` creates a Svelte-aware auth client with nanostore-based reactivity
  - BetterAuth reads BETTER_AUTH_SECRET and BETTER_AUTH_URL from process.env automatically — no need to pass them in config
---

## 2026-04-24 - US-008
- Created sign-up page at `/signup` and sign-in page at `/signin`
- Installed shadcn-svelte card, input, and label components
- Both pages use shadcn-svelte form components (Card, Input, Label, Button)
- Sign-up page has email, password, and confirm password fields with validation
- Sign-in page has email and password fields with validation
- Form validation: email format (regex), password minimum 8 characters, confirm password match
- Error messages displayed inline below each field and for server errors
- Successful sign-up/sign-in redirects to `/` (workspace)
- Cross-links between pages: "Already have an account? Sign in" / "Don't have an account? Sign up"
- BetterAuth client `signUp.email()` and `signIn.email()` used for API calls
- Files changed: src/routes/signup/+page.svelte, src/routes/signin/+page.svelte, src/lib/components/ui/card/*, src/lib/components/ui/input/*, src/lib/components/ui/label/*
- **Learnings for future iterations:**
  - BetterAuth Svelte client exposes `authClient.signUp.email({ email, password, name })` and `authClient.signIn.email({ email, password })`
  - Both return `{ data, error }` — error has `.message` string for display
  - shadcn-svelte Card uses namespace import: `import * as Card from '$lib/components/ui/card'` then `Card.Root`, `Card.Header`, etc.
  - shadcn-svelte Input supports `bind:value` and `aria-invalid` for validation states
  - Use `$derived.by(() => ...)` for validation logic that depends on reactive state
  - Svelte 5 form handling: use `onsubmit` attribute (not `on:submit`) with `e.preventDefault()`
---

## 2026-04-24 - US-009
- Added auth session middleware, sign-out, and protected route layout
- Created `src/hooks.server.ts` — validates BetterAuth session on every request via `auth.api.getSession({ headers })`, populates `event.locals.user` and `event.locals.session`
- Updated `src/app.d.ts` — defined `App.Locals` with typed `user` and `session` (or null)
- Created `src/routes/+layout.server.ts` — passes `locals.user` to all pages via page data
- Updated `src/routes/+layout.svelte` — shows header with user email and sign-out button when authenticated; sign-out calls `authClient.signOut()` then redirects to `/signin`
- Protected routes via `PROTECTED_PREFIXES` array in hooks — `/projects` prefix redirects to `/signin` if unauthenticated; root `/` allows unauth access for temp project
- Session persistence is cookie-based (BetterAuth default behavior)
- Files changed: src/app.d.ts, src/hooks.server.ts, src/routes/+layout.server.ts, src/routes/+layout.svelte
- **Learnings for future iterations:**
  - `auth.api.getSession({ headers: event.request.headers })` returns `{ user, session } | null` — use in hooks.server.ts
  - App.Locals types in app.d.ts must allow null for unauthenticated state
  - Protected routes are cleanest as prefix checks in hooks.server.ts rather than per-route layout guards
  - `authClient.signOut()` from `better-auth/svelte` returns a promise — await before redirecting
  - Layout server load function at root level makes user available to ALL pages via `data.user`
  - Svelte 5 layout: `let { data, children } = $props()` to access both page data and children snippet
---

## 2026-04-24 - US-010
- Created provider registry types and provider interface
- `src/lib/providers/types.ts`: ProviderId type, ProviderRegistryEntry interface, MusicProvider interface with generateTextToMusic/generateInstrumental/generateCoverRestyle methods (plus optional streamGenerationAudio/preprocessCoverSource), and all input/output types (TextToMusicInput, InstrumentalGenerationInput, CoverRestyleInput, ProviderGenerationHandle, ProviderAudioChunk)
- `src/lib/providers/registry.ts`: V1 registry with minimax enabled, elevenlabs/stability as disabled stubs, plus getProvider/getEnabledProviders helpers
- No `any` types used anywhere in provider code
- Typecheck passes (0 errors)
- **Learnings for future iterations:**
  - Provider types at `src/lib/providers/types.ts` define the adapter boundary — all MiniMax (and future) adapters must implement MusicProvider
  - ProviderId ('minimax'|'elevenlabs'|'stability') is separate from the DB schema Provider type which also includes 'local_upload' and 'browser_render'
  - Optional interface methods use `method?()` syntax — adapters only implement streaming if they support it
  - Registry is `readonly` to prevent runtime mutation
---

## 2026-04-24 - US-011
- Created MiniMax request validator at `src/lib/providers/minimax/validateMusicRequest.ts`
- Validates: prompt non-empty, instrumental+lyrics conflict, non-instrumental requires lyrics or optimizer, lyrics length limit (2000 chars), cover/re-style requires source asset, unsupported structure tags produce warnings
- Created `MusicRequestInput` type with mode ('text_to_music'|'instrumental'|'cover_restyle'), prompt, lyrics, instrumental, lyricsOptimizer, structureTags, sourceAssetId
- Returns `ValidationResult` with valid boolean, errors array, and warnings array
- Detects unsupported bracketed tags in lyrics text (not just structureTags array)
- Test file at `src/lib/providers/minimax/validateMusicRequest.test.ts` with 22 test cases covering all acceptance criteria
- Installed vitest (v4.1.5) and added `test`/`test:watch` npm scripts
- All 22 tests pass, typecheck passes (0 errors)
- Files changed: src/lib/providers/minimax/validateMusicRequest.ts, src/lib/providers/minimax/validateMusicRequest.test.ts, package.json, package-lock.json
- **Learnings for future iterations:**
  - vitest works out of the box with SvelteKit's vite config — no separate vitest.config.ts needed
  - `npm test` runs `vitest run` (single pass); `npm run test:watch` runs `vitest` (watch mode)
  - Validator uses `MusicRequestInput` type (not the provider-level `TextToMusicInput`/`CoverRestyleInput`) to support all modes in one function
  - Supported structure tags: [Intro], [Verse], [Verse 1], [Verse 2], [Pre-Chorus], [Chorus], [Hook], [Bridge], [Breakdown], [Solo], [Outro]
  - MiniMax lyrics limit constant: `MINIMAX_MAX_LYRICS_LENGTH = 2000`
  - Cover/re-style mode doesn't require lyrics (they're optional), and skips the non-instrumental lyrics check
---

## 2026-04-24 - US-012
- Created MiniMax adapter at `src/lib/providers/minimax/adapter.ts` implementing `MusicProvider` interface
- Exported payload builder functions: `buildTextToMusicPayload`, `buildInstrumentalPayload`, `buildCoverRestylePayload`
- Defined MiniMax-specific types: `MiniMaxMusicRequest`, `MiniMaxMusicResponse`, `MiniMaxApiError`
- `generateTextToMusic`: sets `is_instrumental=false`, includes lyrics when provided, passes `lyrics_optimization` flag
- `generateInstrumental`: sets `is_instrumental=true`, never includes lyrics
- `generateCoverRestyle`: sets `refer_voice` to source audio URL (full implementation in US-013)
- API key passed via `createMiniMaxAdapter(apiKey)` factory — never exposed client-side
- No `any` types anywhere in adapter code
- `toProviderHandle` maps MiniMax response to typed `ProviderGenerationHandle`
- Created test file at `src/lib/providers/minimax/adapter.test.ts` with 21 tests covering:
  - Payload builder formatting for all three generation modes
  - Lyrics inclusion/omission logic
  - Lyrics optimizer flag passthrough
  - Adapter integration with mocked fetch (correct URL, headers, auth, body)
  - Error handling for HTTP errors and non-zero status codes
  - Response mapping (supportsStreaming, metadata)
- All 43 tests pass (22 existing + 21 new), typecheck passes (0 errors)
- Files changed: src/lib/providers/minimax/adapter.ts, src/lib/providers/minimax/adapter.test.ts
- **Learnings for future iterations:**
  - MiniMax Music API endpoint: `https://api.minimaxi.chat/v1/music_generation`, model: `music-01`
  - MiniMax request uses `is_instrumental` (boolean), `lyrics` (optional string), `refer_voice` (source audio URL for cover)
  - MiniMax response wraps data in `base_resp` (status_code/status_msg) + `data` (task_id, audio/audio_url) + optional `extra_info`
  - `base_resp.status_code === 0` means success; non-zero is an error
  - Use `createMiniMaxAdapter(apiKey)` factory pattern — no class needed, returns `MusicProvider` interface directly
  - Payload builders are exported separately for testability without mocking fetch
  - `vi.spyOn(globalThis, 'fetch')` is the cleanest way to mock fetch in vitest
---

## 2026-04-24 - US-013
- Completed cover/re-style payload mapping in MiniMax adapter
- Added `validateR2SourceUrl()` function to enforce source audio comes from R2 storage, not provider CDNs
- Blocklist covers `cdn.minimax.chat`, `cdn.minimaxi.chat`, `fileserviceupload.minimax.chat`
- `buildCoverRestylePayload()` now validates source URL before building payload — throws on invalid URLs
- Added 22 new tests: R2 URL validation (9 tests), expanded cover payload builder tests (9 tests), expanded adapter integration tests for cover mode (6 tests including provider CDN rejection, lyrics handling, error handling, metadata mapping)
- All 65 tests pass, typecheck passes (0 errors)
- Files changed: src/lib/providers/minimax/adapter.ts, src/lib/providers/minimax/adapter.test.ts
- **Learnings for future iterations:**
  - `validateR2SourceUrl()` at `src/lib/providers/minimax/adapter.ts` rejects known provider CDN domains and requires valid HTTP/HTTPS URLs
  - Cover/re-style uses the same `/v1/music_generation` endpoint as text-to-music, differentiated by `refer_voice` field
  - Provider CDN URLs are temporary — only R2 object keys are canonical source references
  - The validation throws synchronously in `buildCoverRestylePayload`, preventing the API call from being made with invalid source URLs
  - `http:` URLs are allowed for local development (localhost R2 emulation)
---

## 2026-04-24 - US-014
- Implemented streaming hex response handler and error normalization in MiniMax adapter
- Added `ProviderErrorCode` type and `ProviderError` class to `src/lib/providers/types.ts`
- Added `decodeHexToBytes()` — hex-to-Uint8Array decoder with whitespace stripping and validation
- Added `normalizeMiniMaxError()` — maps MiniMaxApiError/network/abort errors to typed ProviderError with codes: provider_auth_error, provider_rate_limited, provider_timeout, provider_validation_error
- Added `parseStreamLines()` — async generator that parses SSE-prefixed or plain JSON-line streaming responses into hex audio chunks, handles [DONE] termination
- Added `streamGenerationAudio` to adapter — streaming mode fetches hex chunks from MiniMax and decodes them; URL fallback fetches audio_url and yields as single chunk
- Wrapped existing `generateTextToMusic`, `generateInstrumental`, `generateCoverRestyle` with error normalization (now throw ProviderError, not MiniMaxApiError)
- Updated existing error tests to verify ProviderError normalization
- Added 39 new tests: hex decoding (6), error normalization (13), stream parsing (8), streaming audio (5+3 URL fallback, 4 error cases)
- All 104 tests pass, typecheck passes (0 errors, 0 warnings)
- Files changed: src/lib/providers/types.ts, src/lib/providers/minimax/adapter.ts, src/lib/providers/minimax/adapter.test.ts
- **Learnings for future iterations:**
  - `ProviderError` is the public error type at the adapter boundary — `MiniMaxApiError` is internal only
  - Error normalization mapping: HTTP 401/403→auth, 429→rate_limited, 408/502/503/504→timeout, 400/1000+→validation
  - `parseStreamLines` handles both SSE format (`data: {...}`) and plain JSON lines
  - `[DONE]` marker terminates SSE streams; `is_final: true` or `data.status === 2` terminates JSON streams
  - URL fallback in `streamGenerationAudio` uses `response.arrayBuffer()` and yields a single `isFinal: true` chunk
  - Mock ReadableStream in tests: use `new ReadableStream({ pull(controller) { ... } })` with TextEncoder
  - `normalizeMiniMaxError` passes through existing `ProviderError` instances unchanged (idempotent)
---

## 2026-04-24 - US-015
- Created asset status state machine at `src/lib/services/asset-status.ts`
- Created job status state machine at `src/lib/services/job-status.ts`
- Both export `validateTransition(current, next)` returning `TransitionResult` (valid or invalid with reason)
- Asset allowed transitions: created→queued, queued→generating/failed, generating→receiving_audio/failed, receiving_audio→persisting/failed, persisting→ready/failed, ready→deleted, failed→deleted
- Job allowed transitions: created→queued, queued→generating/cancelled, generating→receiving_audio/failed, receiving_audio→persisting/failed, persisting→succeeded/failed
- Terminal states: asset deleted, job succeeded/failed/cancelled — no outbound transitions
- Test files cover all allowed transitions, all disallowed transitions, and same-status rejection
- 79 new tests (37 asset + 42 job), all 183 tests pass, typecheck passes (0 errors)
- Files changed: src/lib/services/asset-status.ts, src/lib/services/asset-status.test.ts, src/lib/services/job-status.ts, src/lib/services/job-status.test.ts
- **Learnings for future iterations:**
  - State machine services at `src/lib/services/` — pure functions, no DB dependency
  - `TransitionResult` is a discriminated union: `{ valid: true }` or `{ valid: false; reason: string }`
  - Failed jobs/assets are immutable (except asset failed→deleted). Retry always creates a new job.
  - Asset and job status types imported from `$lib/server/db/schema` — keep in sync if schema changes
---

## 2026-04-24 - US-016
- Created quota reservation service at `src/lib/services/quota.ts`
- Pure `validateQuotaTransition(current, next)` function following state machine pattern
- Allowed transitions: reserved→committed, reserved→released, reserved→expired
- Terminal states: committed, released, expired — no outbound transitions
- `QuotaRepository` interface for DB abstraction with `createDrizzleQuotaRepository(db)` Drizzle implementation
- `createQuotaService(repo)` factory returning `QuotaService` with:
  - `reserveQuota(ownerId, jobId, idempotencyKey)`: creates reservation with 10-minute TTL, idempotent on key+owner
  - `commitQuota(reservationId)`: transitions reserved→committed with validation
  - `releaseQuota(reservationId)`: transitions reserved→released with validation
  - `expireStaleReservations()`: expires all reserved rows past their TTL
  - `checkDailyUsage(ownerId)`: counts committed reservations since midnight UTC today
- In-memory `QuotaRepository` implementation in test file for isolated unit testing
- 46 new tests covering: all transitions (allowed/disallowed/same-status), reserve/commit/release/expire service operations, idempotency key dedup, daily usage counting, full lifecycle scenarios
- All 229 tests pass, typecheck passes (0 errors)
- Files changed: src/lib/services/quota.ts, src/lib/services/quota.test.ts
- **Learnings for future iterations:**
  - Repository pattern enables testing service logic without a real DB — `createInMemoryQuotaRepo()` in test file
  - `QuotaServiceResult` uses `{ ok: true/false }` discriminated union — not exceptions
  - Idempotency key is scoped to owner — same key for different owners creates separate reservations
  - Daily usage counts committed reservations where `updatedAt > todayStartUtc` — uses `updatedAt` because that's when the commit happened
  - Constants exported: `QUOTA_TTL_MS = 600000` (10 min), `DAILY_LIMIT = 10`
---

## 2026-04-24 - US-017
- Created take edge service at `src/lib/services/take-edges.ts`
- Repository pattern: `TakeEdgeRepository` interface, `createTakeEdgeService(repo)` factory, `createDrizzleTakeEdgeRepository(db)` Drizzle implementation
- `createEdge(projectId, ownerId, parentAssetId, childAssetId, branchType, branchPrompt?)`: creates edge with self-edge check and cycle detection
- Cycle detection: BFS from child following parent→child edges to check if proposed parent is reachable
- `getChildren(assetId)`: returns active child edges (filtered by `isNull(deletedAt)`)
- `getParents(assetId)`: returns active parent edges (filtered by `isNull(deletedAt)`)
- `softDeleteEdgesForAsset(assetId)`: sets `deletedAt` on all edges where asset is parent or child
- Soft-deleted edges are excluded from cycle detection queries
- Created test file at `src/lib/services/take-edges.test.ts` with in-memory repository
- 23 tests covering: edge creation (4), self-edge rejection (1), cycle detection (6 — two-node, three-node, four-node cycles, diamond DAG, linear chain, soft-deleted edges ignored), getChildren (3), getParents (3), softDeleteEdgesForAsset (6 — parent/child/both directions, no edges, unrelated assets, idempotency)
- All 252 tests pass (23 new), typecheck passes (0 errors)
- Files changed: src/lib/services/take-edges.ts, src/lib/services/take-edges.test.ts
- **Learnings for future iterations:**
  - BFS cycle detection traverses from the proposed child's descendants (not ancestors) — if the proposed parent is found among descendants of the child, adding the edge would close a cycle
  - `TakeEdgeServiceResult` follows same `{ ok: true/false }` pattern as `QuotaServiceResult`
  - In-memory test repo must filter by `deletedAt === null` to match Drizzle repo's `isNull(deletedAt)` behavior
  - `softDeleteByAssetId` handles both parent and child directions — no need for separate calls
---
